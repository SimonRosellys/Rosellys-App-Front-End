import React from "react";

const CreateSetlist = (show_id) => {
  const handleClick = (e) => {};

  return (
    <div>
      <h1 className="dev" onClick={handleClick()}>
        Create a setlist
      </h1>
      {/*  TODO: THIS WILL CHANGE TO VIEW SET LIST IF ONE IS AVAILABLE */}
    </div>
  );
};

export default CreateSetlist;
